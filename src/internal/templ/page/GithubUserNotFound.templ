package page

import (
    "fmt"
)

templ GithubUserNotFound(name string) {
	<div>
        <p>
            Could not find Github user: "<a href={ templ.URL(fmt.Sprintf("https://github.com/%v", name)) }>{ fmt.Sprintf("https://github.com/%v", name) }</a>"
        </p>
    </div>
}